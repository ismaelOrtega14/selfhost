services:
  actual-budget:
    image: actualbudget/actual-server:latest
    restart: unless-stopped
    ports:
      - "5006:5006/tcp"
    volumes:
      - /home/ismael/actualBudget/data:/data
    healthcheck:
      test: node -e "require('http').get('http://127.0.0.1:5006').on('response',()=>process.exit(0)).on('error',()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

networks:
  default:
    name: pangolin_net
